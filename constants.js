// strategy responses
var S = 'Stand';
var H = 'Hit';
var Dh = 'Double (else hit)';
var Ds = 'Double (else stand)';
var P = 'Split';
var Ph = 'Split if you can double after split (else hit)';
var Pd = 'Split if you can double after split (else double)';
var Ps = 'Split if you can double after split (else stand)';
var Rh = 'Surrender (else hit)';
var Rs = 'Surrender (else stand)';
var Rp = 'Surrender (else split)';

// strategy struct for later drawing
var strategies = [];
